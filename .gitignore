# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Env / secrets
.env
IGNORE/api-keys.txt

# Pipeline run logs (reproducible from code, not tracked)
logs/runs/

# target-repo's own nested .git was moved aside so its files push as
# regular files in this repo; don't track the leftover backup.
target-repo/.git.bak/

#Skills
.claude/
.agents/

# OS
.DS_Store
Thumbs.db
